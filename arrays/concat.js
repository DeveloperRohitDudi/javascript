let firstList = [1, 2, 3, 4];
let secondList = [5, 6, 7];
console.log(firstList.concat(secondList));

// for (let i = 0; i < secondList; i++) {
//     firstList.push(secondList[i]);
// }
// console.log(firstList);
